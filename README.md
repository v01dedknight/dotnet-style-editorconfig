# C# EditorConfig (2-space style)

Конфиг для автоматического соблюдения стайлгайда в C# проектах через `.editorconfig`.

Работает в Visual Studio, Rider и других IDE с поддержкой EditorConfig.

## Что настраивается

- Отступы 2 пробела
- Скобки на той же строке
- lowerCamelCase для локальных переменных
- Запрет на использование `var`
- Ошибки подсвечиваются как `error`

## Как использовать

1. Скопируйте файл `.editorconfig` в корень вашего проекта (через Solution Explorer).
2. Убедитесь, что файл лежит на уровне `.sln`.
3. Когда код готов — нажмите

   Ctrl + K, Ctrl + D

IDE автоматически
- выставит отступы в 2 пробела
- приведёт скобки к нужному виду
- подсветит нарушения нейминга
- подсветит использование `var`

---

## 🤝 Контрибьюция

Хочешь улучшить макрос?

Будем рады твоему pull request!  
Делай форк, добавляй улучшения, открывай обсуждения.

---

## Как сделать Pull Request (PR)

### 1. Форкните репозиторий

Нажмите кнопку **Fork** в правом верхнем углу на GitHub.

---

### 2. Клонируйте форк к себе

```bash
git clone https://github.com/ВАШ_ЛОГИН/ИМЯ_РЕПОЗИТОРИЯ.git
cd ИМЯ_РЕПОЗИТОРИЯ
```

---

### 3. Создайте новую ветку

```bash
git checkout -b имя-вашей-ветки
```

---

### 4. Внесите изменения

Внесите необходимые изменения в файлы проекта
(например, `.editorconfig`, `README.md` или другие конфигурационные файлы).

Рекомендуется:

- Сохранять существующий стиль и структуру
- Делать изменения атомарными (одна логическая правка — один коммит)
- Не добавлять лишние файлы или временные артефакты
- Проверить, что конфиг корректно работает в IDE

---

### 5. Зафиксируйте изменения

```bash
git add .
git commit -m "Описание изменений"
git push origin имя-вашей-ветки
```

---

### 6. Создайте Pull Request

1. Перейдите на GitHub  
2. Нажмите **Compare & pull request**  
3. Выберите нужную ветку  
4. Создайте PR в ветку `main`

---

Если хотите ускорить проверку — можете написать мне в VK / TG 🙂

---

## 💡 Советы

- Проверьте, что код работает.
- Убедитесь, что нет лишних изменений или временных файлов.
- Коммиты должны быть понятными и осмысленными.